# Session Handoff — 2026-05-02

新窗口打开后先读这份，能 30 秒接上当前状态。

---

## 1. 必读文档（按顺序）

1. **`docs/SCALING_LAW_LOG_2026_05_02.md`** ★ — 当前最新研究日志，12 节，包含：
   - fire_patched cache fusion_tag bug 的修复与恢复
   - strongreg / ep1 假设被否决
   - **12y 是 sweet spot**（SOTA = 12y enc21 = 7.834x Lift@5000，6.727x Lift@30km）
   - SSD-copy bug（22y 4 个 job timeout，96 GPU-h 浪费）已修
   - recent-data baseline = 5.31x（比 climatology 4.42x 强 20%）
   - calibration-vs-rank 假设（待 lift_trajectory 实验验证）
2. `docs/SCALING_LAW_LOG_2026_04_25_26.md` — 上一段日志
3. `docs/LABEL_DECISION_2026_04_21.md` — NBAC+NFDB 标签切换的依据
4. `~/.claude/projects/-Users-huangjiaqi-Desktop-wildfire-refactored/memory/MEMORY.md` — 用户长期记忆

---

## 2. 当前 SOTA（paper-ready）

val 期 2022-2025，NBAC+NFDB 标签：

| Model | Lift@5000 | Lift@30km |
|---|---|---|
| **12y enc21** ★ | **7.835x** [7.50, 8.21] | **6.727x** [6.40, 7.07] |
| 12y enc14 | 6.405x [6.17, 6.68] | 5.076x [4.81, 5.36] |
| 12y enc28 | 5.579x ⚠️ | n/a |
| 12y enc35 | 5.615x ⚠️ | n/a |
| 22y enc35 | 4.91 | n/a |
| 22y enc14 | 3.88 | n/a |

⚠️ enc28/35 行的 n_fire == n_win，可能 fire-window 过滤逻辑没生效，paper 出图前要 verify。

**Baseline lock**：
- climatology = **4.42 ± 1.61x** Lift@5000（standalone benchmark_baselines.py，646 win，定稿）
- recent-data = **5.31x** Lift@5000（recent_logreg_baseline.py，2022 训 / 23-24 测）
- ⚠️ Unified script 里那个 7.33 climatology **不可信，已弃用**（33 天 max-agg 膨胀）

12y enc21 vs climatology = +77%，vs recent-data = +47%。

---

## 3. 队列中的 job（2026-05-02 时点）

| Job ID | 内容 | 状态 | 关键产出 |
|---|---|---|---|
| 60178279 | 12y 13ch cache build | R | 解锁 12y×13ch sweep |
| 60178280 | 12y 16ch cache build | R | 解锁 12y×16ch sweep |
| 60179163 | 12y enc28 save_scores | PD | 完整 4-enc 表 |
| 60179164 | 12y enc35 save_scores | PD | 完整 4-enc 表 |
| 60179165-168 | 22y_ep1 × 4 save_scores | PD | 22y_ep1 全量评估 |
| **60185099** | **lift_trajectory（22y enc14 mid-epoch eval）** | **PD ~36h** | **验证 calibration-vs-rank 假设** |
| 60185107-09 | 12y enc14 recency tau=6/10/15 | PD 12h each | 看 recency 能否再推高 12y |
| 60185573 | 22y 13ch NBAC+NFDB prep | PD 48h | 解锁 22y×13ch sweep |

下次 ssh narval 第一件事：`squeue -u jiaqi217` 对一下哪些跑完了。

---

## 4. 已确认可用的修复

- ✅ `train_v3.py` fire_patched cache filename 含 fusion_tag（2 个单测 + audit 脚本）
- ✅ 4 份 SLURM 脚本（22y_strongreg / 22y_recency / 12y_recency / lift_traj）都加了 SSD copy block（`LOCAL_METEO=$LOCAL_CACHE/meteo` 然后 `cp` 整个 `$CACHE_DIR_LUSTRE/*`）
- ✅ stale 4y/12y/4y_13ch 的 buggy `.dat` 已删
- ✅ recency 训练通路（FocalBCELoss 加 sample_mean，_RecencyWeightedDS wrapper，--recency_tau / --recency_base_year）
- ✅ mid-epoch val（--mid_epoch_val_every）
- ✅ W&B offline 接好（已登录 narval）

---

## 5. 待办（按优先级）

**等 job：**
1. lift_trajectory 跑完 → 看 Lift@5000 在 ep1 中段是否有峰 → 判定 calibration-vs-rank 假设。如果有峰，就重新评估 22y 在 peak step 的 ckpt
2. 12y enc28/35 save_scores → 4-enc 完整表（可能 enc28 比 enc21 还高，要小心 SOTA 名义会变）
3. 12y_recency × 3 → 看是否能把 12y enc14 推过 7.0x

**手上可立即做：**
- 写 `scripts/recall_at_budget.py`（log §9 TODO）：top X% 加拿大面积 = 1/5/10%，看召回多少 fire event
- 起草 paper §3 method
- 把 12 模型 scaling 表（4y / 12y / 22y × enc14/21/28/35）整理成最终 figure

**不会做：**
- ✗ 22y_strongreg（已弃）
- ✗ 22y_recency（已弃，除非 12y_recency 出彩才回来重试）
- ✗ MAE / SSL pretraining（defer）
- ✗ 架构变化（先看 trajectory 结果再决定）

---

## 6. 工程注意

- Narval 脚本上 cache 必须先 cp 到 `/localscratch/$SLURM_TMPDIR/cache/meteo` 再训，**直接指 Lustre 会 100x 慢**（fancy indexing seek pattern）
- 任何新训练脚本对 `meteo_patched[c0:c1, t_indices, :]` 都要走 SSD
- climatology 数字以 `benchmark_baselines.py` 为准，不要混用 unified 那个
- 标签变更后旧 CWFIS-label 的 SOTA 数字（6.47x）**不可比**

---

## 7. 接续提示词模板

新窗口可以直接说：

> 读一下 `docs/HANDOFF_2026_05_02.md` 和 `docs/SCALING_LAW_LOG_2026_05_02.md`，然后 ssh narval 看 squeue 状态。
