# HANDOFF 2026-05-31 (paper figures + SOTA session)

Read this first in the next session. Companion: `~/.claude/.../memory/paper_figures_status.md`.

## TL;DR
This session built the full paper figure set (Fig 1, 2, 2-B, 2-F, 2-G, 2-H, 3, 4a/4b)
from real full-window leak-free NBAC+NFDB eval data, and found a NEW SOTA ensemble.
Remaining work is mostly "wait for jobs → pull → refresh figures".

## NEW SOTA (headline number for the paper)
**11-member prob-mean ensemble = 9.63× Lift@5000 / 8.45× Lift@30km**
(= old 10 ckpts + per_pixel-gating ckpt). Beats old 10-ckpt prob-mean (9.57 / 4.37 —
30km had COLLAPSED) on BOTH scales. Adding the gating member fixed the 30km smoothing
collapse. File: `outputs/ensemble_11_gating_prob_mean.json` (logit variant 9.08/8.41).

## Figures DONE (scripts in scripts/plot_*.py, outputs in figures/)
- Fig 1  per-lead Lift            plot_fig1_per_lead.py
- Fig 2  aggregate bars           plot_fig2_aggregate_bars.py  (persistence framing: see note)
- Fig 2-B multi-metric (2x2, 7 models) plot_fig2b_multimetric.py — ensemble F2=0.475 best
- Fig 2-F per-year Lift           plot_fig2f_per_year.py
- Fig 2-G per-year novel          plot_fig2g_novel_ignition.py
- Fig 2-H cross-model novel (7 models) plot_fig2h_novel_crossmodel.py — KEY finding below
- Fig 3  Canada map (SOTA, 2023-05-15) plot_fig3_canada_map.py
- Fig 4a data scaling / 4b model scaling plot_fig4_scaling.py (PRELIMINARY 20-win; see jobs)
Shared style: scripts/plot_paper_style.py. Eval data: results/eval/.

### Fig 2-H key finding (strong paper argument)
Novel-30d Lift@5000: ensemble 8.2 > SOTA 7.1 > climatology 6.3 > MLP 5.9 > ConvLSTM 5.0;
persistence 20.6 standard → 0 novel. ONLY ensemble+SOTA beat the climatology prior;
MLP/ConvLSTM fall BELOW it → transformer is the only learned arch that truly forecasts
NEW fires. Persistence's standard-Lift dominance is a degenerate artifact of NBAC
multi-week mega-fire persistence.

## JOBS RUNNING / PENDING on Narval (as of session end)
- 62042270-274  wf-loyo          RUNNING ~11h  — LOYO 5 folds (val 2020..2024). Training jobs.
- 62088478-481  wf-12y-encsweep  PENDING       — enc 7/10/42/56 training (Fig 4c). SOTA recipe, only in_days varies.
- 62088143-145  wf-scaling-eval  PENDING       — 14y/16y/22y FULL eval (Fig 4a paper-grade). ⚠ VERIFY they use SKIP_COPY (see risks).

## WHAT TO DO NEXT (when jobs finish)
1. **LOYO** (most important — paper robustness). When 62042270-274 done:
   - Folds save per-window JSON; aggregate with `python3 scripts/aggregate_loyo.py` ON NARVAL.
   - Produces loyo_summary.csv (macro+micro CI). Pull → make robustness figure + rewrite
     §5.3 Evaluation (forward-chaining LOYO + bootstrap CI). Task #8.
2. **Encoder sweep → Fig 4c**. When 62088478-481 done:
   - Pull outputs/v3_9ch_enc{7,10,42,56}_12y_2014_per_window.json to results/eval/scaling/.
   - Existing pts already local: enc14/21/28/35. Add new 4 → 8-point Fig 4c.
   - Story: pixel-Lift peaks short (enc14=6.53), cluster-Lift peaks mid (enc21-28);
     enc21 = balance = SOTA. Supports MEMORY lesson "enc7→21 +2.17×". Task #21.
3. **Scaling Fig 4a paper-grade**. When 62088143-145 done:
   - Pull v3_9ch_enc21_{14y_2012,16y_2010,2000}_FULL_per_window.json.
   - Already have 4y/10y(=6y train)/12y(=8y train,=SOTA) full-window. Update plot_fig4_scaling.py
     to read *_FULL_per_window.json. Task #17.
4. **Refresh Fig 2 / 2-B / 2-H with 9.63× ensemble** if not already (Task #20, low pri).

## ⚠️ RISKS / BUGS TO FIX NEXT SESSION
1. **Label slices STILL broken**: slurm/extract_label_slices_narval.sh writes to /tmp/
   on Narval (ephemeral, lost on job exit). I fixed it LOCALLY (commit pushed, line 27 now
   outputs/fire_actual_) but **Narval git pull is silently failing** so the remote file is
   still the /tmp version. Job 62088386 "succeeded" but produced nothing. These slices feed
   Fig 3 alternate dates (2022-08-15, 2023-08-15) — OPTIONAL (main 2023-05-15 map is done).
2. **Narval git is stuck**: `git rev-parse HEAD` shows 2f12fd9 (behind local main), git pull
   and git fetch produce NO output (silently fail). The `git` module sometimes "disappears".
   FIX NEXT TIME: `cd /scratch/jiaqi217/wildfire-refactored && module load StdEnv/2023 git &&
   git fetch origin && git reset --hard origin/main` (or debug why pull is silent). UNTIL
   FIXED, any new slurm script edit won't reach Narval.
   - NOTE: SLURM snapshots the script at sbatch time, so already-submitted jobs
     (encsweep/scaling) run their submit-time script version regardless of later git state.
     But VERIFY scaling 62088143-145 were submitted WITH SKIP_COPY=1 (else they'll timeout
     again at 14h like 62038733/734 did). If they timeout, resubmit with:
     `RANGE_TAG=14y_2012 DATA_START=2021-05-01 PRED_END=2025-09-23 FORCE_MASTER=1 SKIP_COPY=1 sbatch slurm/eval_scaling_full_narval.sh`
3. **SSH/MFA unstable**: many commands this session got ConnectTimeout or empty output.
   Use `ssh -o ConnectTimeout=30`, run ONE command at a time (parallel ssh calls cancel
   each other), re-run if empty.

## DEAD ENDS (do NOT retry — confirmed this session)
- spatial-negative mining (R100/200/300): all BELOW baseline (5.61/4.32/2.92 vs 6.10). DEAD.
- teleconnection-as-channel: data downloaded (data/climate_indices/teleconnections_daily.csv,
  ONI/AO/NAO/PDO) but integration deferred — judged near-null for within-window Lift (a
  within-window constant barely changes within-window ranking). MJO not obtained (BoM blocks).
- 22y data, anomaly loss, decoder-only, d_model>256, enc>21 (for Lift@5000), single-epoch,
  strong reg, global/per_lead gating, naive recency.
- LESSON: SOTA-audit subagent flagged spatial-neg etc. as "underexplored" but they were
  already DONE. ALWAYS check checkpoints/outputs before submitting a "new" experiment.

## DATA FACTS (full-window, leak-free, NBAC+NFDB, 435 win)
- SOTA single: Lift@5000=7.83, Lift@30km=6.73, F2=0.452, BSS≈0, ROC-AUC=0.900
- ensemble 11+gating: 9.63 / 8.45 (NEW best)
- persistence: 20.3 standard → 0 novel (degenerate)
- climatology: 7.04 (NOT 4.42 from old MEMORY — that was a different eval config)
- fwi_oracle/threshold ≈ 0 (geographic mismatch w/ NBAC)
- Data scaling: inverted-U, peak ~12y (team naming), collapse at 22y → recency > volume
- Model scaling: saturates by d_model=256 → data-limited not capacity-limited

## CAPTION/PROSE DRAFTS WRITTEN (in conversation, not yet in a file)
Wrote no-em-dash English caption+discussion paragraphs for: scaling (Fig 4a), Fig 2-B,
Fig 3, Fig 2-H. User wants NO em-dashes (—). Consider collecting into docs/figure_captions.md.

## PERSISTENCE FRAMING (pending user decision for Fig 2)
Fig 2 aggregate: persistence wins standard Lift (degenerate). User never finalized whether
to (a) include persistence honestly + lead with novel/F2/30km, (b) switch headline to
F2/BSS/30km, or (c) omit persistence. Current figs include it. Fig 2-B + 2-H already handle
this honestly. Likely just need a caption decision.
