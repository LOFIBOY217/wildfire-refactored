# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Large data, model, and output artifacts — never commit these.
# Prevents accidental `git add`/`git stash -u` of multi-GB data and checkpoints.
data/
outputs/
checkpoints/
*.dat
artifacts/

# Virtual envs
.venv/
venv/

# Personal notes (not for repo)
notes/

# Internal running-state handoff notes (kept local, not for public repo)
HANDOFF.md
HANDOFF_*.md
docs/HANDOFF_*.md

# AI-assistant working context (kept local, not for public repo)
CLAUDE.md
